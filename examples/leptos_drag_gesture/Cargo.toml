[package]
name = "leptos_drag_gesture"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
animato = { path = "../../crates/animato", features = ["leptos-csr"] }
leptos = { version = "0.8.19", features = ["csr"] }
