[package]
name = "devtools_bevy_egui"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
animato = { path = "../../crates/animato", features = ["std", "bevy", "devtools-egui-panel"] }
